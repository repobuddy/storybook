---
"@repobuddy/storybook": minor
---

Add negation-style tag names to tag badges addon

Extend the `TagNames` type with `!test`, `!type`, `!func`, `!var`, and `!snapshot` for use in Storybook stories.
