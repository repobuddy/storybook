---
"@repobuddy/storybook": minor
---

Add `ExtendsStoryObj`, deprecate `ExtendStoryObj`.

`ExtendsStoryObj` simplify type params, and improve extending `tags` if the extending `StoryObj` has predefined tag literals.
