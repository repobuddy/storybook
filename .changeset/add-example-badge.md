---
"@repobuddy/storybook": minor
---

Add example badge to storybook-addon-tag-badges

- Add `exampleBadge` for the `example` tag (✨ with "Example" tooltip)
- Add `example_badge.stories.tsx` demonstrating the badge
