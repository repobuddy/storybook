---
"@repobuddy/storybook": patch
---

Update `tailwindcss` to 4.2.1 and change it to direct dependency.
This is needed to support composing Tailwind config using `@repobuddy/storybook/tailwind.css`.
