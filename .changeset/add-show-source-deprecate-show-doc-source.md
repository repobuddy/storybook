---
"@repobuddy/storybook": minor
---

Add `showSource` decorator for displaying story source above or below the rendered story. Deprecate `showDocSource` in favor of `showSource({ placement: 'after' })`.
