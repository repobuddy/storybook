---
"@repobuddy/storybook": minor
---

Add source badge to storybook-addon-tag-badges.

- Export `sourceBadge` tag badge parameter for the `source` tag (displays `</>` with "Source Code" tooltip, hidden in MDX).
