---
"@repobuddy/storybook": minor
---

Add `ExtendsMeta`, deprecate `ExtendMeta`.

`ExtendsMeta` simplify type params, and improve extending `tags` if the extending `Meta` has predefined tag literals.
