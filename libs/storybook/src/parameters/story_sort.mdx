import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="parameters/storySort" />

# storySort

This MDX page shares the same title as `story_sort.stories.tsx`.
Because its type is `docs`, `compareByType` places it before every
`story`-type entry in this group.
