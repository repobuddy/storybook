import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="parameters/storySort/examples" />

# storySort – examples

A sub-path of `parameters/storySort`. All three sub-paths (`examples`,
`migration`, `wip`) are unlisted in the `order` array, so they fall back
to alphabetical sorting. `examples` sorts first.
