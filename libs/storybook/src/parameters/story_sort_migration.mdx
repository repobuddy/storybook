import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="parameters/storySort/migration" />

# storySort – migration

A sub-path of `parameters/storySort`. Sorts after `examples` and before
`wip` alphabetically.
