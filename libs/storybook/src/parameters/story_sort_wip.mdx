import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="parameters/storySort/wip" />

# storySort – wip

A sub-path of `parameters/storySort`. `wip` sorts last among the three
sub-paths alphabetically.
