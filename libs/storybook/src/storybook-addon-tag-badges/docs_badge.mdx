import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="storybook-addon-tag-badges/docsBadge" />

# Docs badge

Use the **docs** badge to mark stories that are primarily documentation. The badge displays 📖 with a "Documentation" tooltip.

## Usage

Add the `docs` tag to your story.

```tsx
export const YourStory = {
	tags: ['docs'],
	render: () => <YourComponent />
}
```
